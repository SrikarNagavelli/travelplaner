export interface Experience {
  id: string;
  destinationId: string;
  title: string;
  description: string;
  imageUrl: string;
  category: string;
  price: number;
  duration: string;
  rating: number;
  reviewCount: number;
}